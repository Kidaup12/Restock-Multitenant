{
  "name": "@wezesha/ws-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone WebSocket gateway: Redis pub/sub in, tenant-scoped sockets out",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/index.cjs --sourcemap --external:bufferutil --external:utf-8-validate",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@wezesha/realtime": "*",
    "ioredis": "^5.11.1",
    "tsx": "^4.23.1",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.25.0",
    "typescript": "^5",
    "vitest": "^4.1.7"
  }
}
